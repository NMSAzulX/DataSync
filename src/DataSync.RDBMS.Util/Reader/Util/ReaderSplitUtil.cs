using System.Collections.Generic;
using Microsoft.Extensions.Configuration;

namespace DataSync.RDBMS.Util.Reader.Util
{
    public static class ReaderSplitUtil
    {
        public static List<IConfiguration> Split(IConfiguration configuration, int adviceNumber)
        {
            throw new System.NotImplementedException();
        }
    }
}