using System.Data;
using Microsoft.Extensions.Configuration;

namespace DataSync.RDBMS.Util
{
    public static class DBUtil
    {
        public static void ConfigureSession(IDbConnection conn, IConfiguration configuration, DataBaseType dataBaseType)
        {
        }
    }
}